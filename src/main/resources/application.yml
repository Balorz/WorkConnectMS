spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/workcon
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
